    //first problem
const doble = (arr) => arr.map((val) => val * 2);

    //second problem

const squareAndFindEvens = numbers => numbers.map(num => num ** 2).filter(square => square % 2 === 0)
//this is skipping the third element in my array...why??